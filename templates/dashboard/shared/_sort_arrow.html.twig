{% macro sortArrow(sortName, sort, sortDirection) %}
    {% if sort == sortName %}
        {% if sortDirection == 'asc' %}
            <twig:ux:icon name="wba:c-up" class="inline-block w-4 h-4 fill-none stroke-current"/>
        {% else %}
            <twig:ux:icon name="wba:c-down" class="inline-block w-4 h-4 fill-none stroke-current"/>
        {% endif %}
    {% else %}
        <twig:ux:icon name="wba:up-down" class="inline-block w-4 h-4 fill-none stroke-current"/>
    {% endif %}
{% endmacro %}
