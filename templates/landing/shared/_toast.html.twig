<div {{ stimulus_controller('toast') }} {{ stimulus_target('toast', 'toast') }}>
    <div class="{{ toast_type }}">{{ message }}
        <button class="ml-auto" {{ stimulus_action('toast', 'close') }}>
            <twig:ux:icon name="wba:cross" class="fill-none"/>
        </button>
    </div>
</div>
