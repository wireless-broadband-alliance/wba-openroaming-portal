{% extends '/landing/base.html.twig' %}

{% block title %}
    {{ data.title.value }} - {{ 'termsConditions'|trans({}, 'tos') }}
{% endblock %}

{% block second_col %}
    <div>
        {{ content|raw }}
    </div>
{% endblock %}
