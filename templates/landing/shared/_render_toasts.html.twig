<div class="grid grid-cols-1 gap-[5px] my-3">
    {# Success Warning Landing #}
    {% for message in app.session.flashbag.get('success') %}
        {{ include('landing/shared/_toast.html.twig', {toast_type: 'toast-success'}) }}
    {% endfor %}

    {# Warn Warning Landing #}
    {% for message in app.session.flashbag.get('warning') %}
        {{ include('landing/shared/_toast.html.twig', {toast_type: 'toast-warning'}) }}
    {% endfor %}

    {# Error Warning Landing #}
    {% for message in app.session.flashbag.get('error') %}
        {{ include('landing/shared/_toast.html.twig', {toast_type: 'toast-error'}) }}
    {% endfor %}
</div>
