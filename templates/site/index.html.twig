{% extends 'base.html.twig' %}

{% block title %}TCS OR Onboarding{% endblock %}

{% block body %}
    <div class="flex flex-col items-center justify-center min-h-screen bg-gray-100">
        <h1 class="text-4xl font-bold text-center mb-8">Welcome to TCS OR Onboarding</h1>
        <p class="text-xl text-center mb-8">This page is so top secret, even the NSA doesn't know about it!</p>
        <div class="flex flex-col items-center space-y-4">
            <a href="{{ path('app_login') }}"
               class="px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-md">Login</a>
            <a href="{{ path('app_register') }}"
               class="px-4 py-2 bg-green-500 hover:bg-green-600 text-white rounded-md">Register</a>
            <a href="{{ path('profile_android') }}"
               class="px-4 py-2 bg-yellow-500 hover:bg-yellow-600 text-white rounded-md">Download Android Profile</a>
        </div>
    </div>
{% endblock %}
