<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Template</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#080D4A]">

    <div class="container mx-auto px-8 pb-8 pt-[50px]">
        <div class="bg-white rounded-[18px] p-6 shadow-md">
            {% if is2FATemplate %}
            <h1 class="text-3xl font-bold">OpenRoaming Two Factor Authentication</h1>
            <p class="mt-6 text-lg">Your two factor authentication code is: <span class="font-bold"> {{ verificationCode }} </span></p>
            <p class="mt-2 text-lg">This code can only be used once and is valid for the next <span class="font-bold"> {{ secondsLeft }} </span> seconds.</p>
            <p class="text-lg">Please keep this code confidential and do not share it with others.</p>
            <p class="mt-6 text-lg">
                Thank you,<br>
                OpenRoaming Portal Support Team
            </p>
            {% else %}
            <p class="text-lg">Your authentication code is: <span class="font-bold"> {{ verificationCode }} </span></p>
            <p class="text-lg">Please keep this code confidential and do not share it with others.</p>
            {% endif %}
        </div>
    </div>

</body>
</html>
