<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm your Password Reset</title>
</head>
<body style="background-color: #080D4A; font-family: ui-sans-serif, system-ui, sans-serif; color: #4A5568; margin: 0; padding: 0;">
<div style="max-width: 600px; margin: 0 auto; padding: 3rem 1rem;">
    <div style="max-width: 448px; margin: 0 auto; background-color: white; border-radius: 1rem; box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1); overflow: hidden;">
        <div style="background-color: #7BBA29; color: white; text-align: center; padding: 1.5rem;">
            {% if resetPassword %}
                <h1 style="font-size: 1.5rem; font-weight: bold;">Admin Password Reset</h1>
            {% else %}
                <h1 style="font-size: 1.5rem; font-weight: bold;">Admin Settings Reset</h1>
            {% endif %}
        </div>

        <div style="padding: 1.5rem;">
            {% if resetPassword %}
                <p style="font-size: 1.125rem; margin-bottom: 1rem;">This is a step to confirm your password reset.</p>
            {% else %}
                <p style="font-size: 1.125rem; margin-bottom: 1rem;">This is a step to confirm the settings reset.</p>
            {% endif %}

            <p style="font-size: 1.125rem; margin-bottom: 1rem;">
                Please use the following code to confirm the action:
                <span style="font-weight: bold; color: #0045AD;">{{ verificationCode }}</span>
            </p>
        </div>
    </div>
</div>
</body>
</html>
