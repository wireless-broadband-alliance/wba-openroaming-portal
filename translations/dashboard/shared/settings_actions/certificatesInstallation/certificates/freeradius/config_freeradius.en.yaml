warning:
    root_command: 'Run the following commands as the root user in your terminal before starting!'
confirm_follow_steps: 'Are you sure you want to proceed to apply the previous uploaded certificates and the new configuration on the resolver?'
submit: 'Confirm Configuration'
